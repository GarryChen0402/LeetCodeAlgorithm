#include "header.h"
class Solution {
public:
    int minOperations(string s) {
        int r = 0;
        int ans = INT_MAX;
        int tmp = 0;
        int n = s.size();
        for(int i=0;i<n;i++){
            
        }
        return ans;
    }
};